# Win2 ToDo

## A. 基盤設定（Wix × Google Sheets）

* [x] **Secrets 設定**

  * `velo-spreadsheet-credentials`（GCPサービスアカウントJSON）
  * `G_SHEETS_ID`（対象スプレッドシートID）
* [x] **シート共有**：対象スプレッドシートをサービスアカウントの `client_email` に共有（編集可）
* [x] **クリックログ動作確認**

  * `backend/gSheetsLogger.jsw` にて `appendValues()` を**一次元配列**で呼び出す形に修正
  * 「クリック」→ **「クリックログ」A:C** に追記されることを確認

## B. 案件一覧（フロントエンド）

* [x] **会員ID取得 & 動的リンク生成**

  * `wix-members-frontend` で `memberId` を取得
  * `baseLink + "?id1=<memberId>"` を生成し遷移
* [x] **クリック時ログ記録**

  * `logClick({ memberId, dealName })` を呼び出し、成功/失敗をログ出力
* [ ] **ASP差異の吸収（NEW）**

  * [ ] CMSコレクション `AffiliateDeals` に **`paramKey`**（文字列）列を追加
  * [ ] 既存ページコードを **`id1` 固定 → `paramKey`（空なら `id1`）採用**に改修
  * [ ] 案件ごとにASP仕様に合わせた `paramKey`（例：`s1`, `sub_id` 等）を入力
  * [ ] 公開環境で、URLが `...?paramKey=<memberId>` となっているか確認

## C. ASP 運用（サブID＝会員IDをCSVに載せる）

* [ ] **ASPごとに確認**

  * [ ] サブID/トラッキングパラメータの**正式キー名**を調査（`id1` / `s1` / `sub_id` など）
  * [ ] ASPレポートに**そのパラメータ列が出力される設定**になっているか確認
  * [ ] （必要なら）ASPの「レポート項目」に `paramKey` を登録
* [ ] **案件登録の運用ルール**

  * [ ] 案件の `baseLink` と `paramKey` をCMSに登録（**必須**）
  * [ ] テスト申込 → ASPの成果画面/CSVで **会員ID列が出る**ことを確認

> ※ 難しいASPが出た場合の**代替**
>
> * [ ] **HTTP関数ゲートウェイ（任意）**：`/_functions/aff?dealId=XXX&id1=...` を受けて、ASPの期待キー名に**付け替え**て302リダイレクト（フロントは常に `id1` でOK）

## D. GAS（スプレッドシート側・集計）

* [x] **Apps Script `code.gs` 設定**（提示済みコードを貼付）
* [ ] **シート構成の最終確認**

  * [x] `クリックログ`（A:C＝日時/ memberId/ dealName：運用中）
  * [ ] `成果CSV_RAW`（ASPのCSVを**ヘッダ込み**で貼付）
  * [ ] `成果データ`（出力先。スクリプトが自動整形）
  * [ ] `会員リスト`（`memberId` と `氏名` を最低限用意）
* [ ] **手動運用フローの定着**

  * [ ] ASPから対象期間の**CSVをDL**
  * [ ] `成果CSV_RAW` に**貼付（ヘッダ込み）**
  * [ ] メニュー **「成果処理 > CSV取込→集計」** をクリック
  * [ ] `成果データ` で **承認のみ支払い（20%・切り捨て）** を確認
* [ ] **自動化（任意）**

  * [ ] `setupTrigger()` を一度実行 → **毎日 03:10** 自動集計
  * [ ] CSV自動投入（Drive連携・RPA等）を検討すると**完全自動**に近づく
* [ ] **微調整（必要に応じて）**

  * [ ] 丸めを**四捨五入**に変更したい → `Math.floor` → `Math.round`
  * [ ] `BATCH_SIZE` 最適化（大量CSVでタイムアウトする場合は 100–300）
  * [ ] `HEADER_CANDIDATES` をASPの実ヘッダに合わせて追記

## E. 検証・QA

* [ ] **クリック→ログ追記→ASP→CSV→集計 のエンドツーエンド**

  * [ ] テスト会員で実クリック → `クリックログ` 追記OK
  * [ ] ASP成果レポートCSVに **会員ID列が存在**（`paramKey`に一致）
  * [ ] `runImportAndAggregate` 実行 → `成果データ` に **氏名/案件名/承認状況/キャッシュバック金額** が出力
  * [ ] 承認＝支払対象、未承認＝0円 のルールが反映
  * [ ] 案件名がCSVに無い場合でも、**クリックログの最新値**で補完される

## F. ドキュメント化（配布/引継ぎ）

* [ ] **運用マニュアル**（社内向け）：

  * 案件登録（`baseLink`/`paramKey`）手順
  * 日々のCSV取込～集計～支払の流れ
  * トラブルシュート（CSVに会員IDが無い／案件名が不明／計算が合わない 等）
* [ ] **技術メモ**：

  * Secrets名、サービスアカウント、共有手順
  * `gSheetsLogger.jsw` のI/F
  * GASのパラメータ（シート名・丸め・承認ルール・BATCH_SIZE）

---

## いま完了しているところ ✅

* ✅ Secrets 設定（`velo-spreadsheet-credentials` / `G_SHEETS_ID`）
* ✅ スプレッドシート共有（サービスアカウントに編集権）
* ✅ `gSheetsLogger.jsw` による **クリックログの追記**（一次元配列でOK）
* ✅ 案件一覧ページで **会員ID付与リンク**生成＆クリック時の**ログ記録**

## 次にやること（優先度順）▶

1. **CMSに `paramKey` を追加** → ページコードを `paramKey` 対応に改修
2. **ASP側設定を整備**（`paramKey` に一致する列がCSVに出るように）
3. GAS側の**運用確立**：`成果CSV_RAW` へ貼付 → メニュー実行 → 出力確認
4. 必要に応じて **トリガー自動化**、HTTP関数ゲートウェイ（任意）

---

このToDoをベースに、ひとつずつ潰していけば**エンドツーエンドで運用が回る状態**になります。
次は「CMSに `paramKey` を追加 → ページコード改修」の差分を用意しましょう。必要なら**改修済みの全文コード**もすぐ出します。
